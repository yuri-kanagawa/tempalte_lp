import { LANGUAGE } from 'src/constants/language'

export const scroll = {
  app: 'app',
  contact: 'contact',
  howToUse: 'howToUse',
  download: 'download'
}
export type ScrollKey = keyof typeof scroll
