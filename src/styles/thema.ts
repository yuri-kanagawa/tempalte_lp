import { createTheme } from '@mui/material'

export const theme = createTheme({
  palette: {
    primary: {
      main: '#000000',
      contrastText: '#FFFFFF'
    },
    secondary: {
      main: '#bdbdbd',
      contrastText: '#616161'
    },
    background: {
      default: '#000000'
    }
  }
})
